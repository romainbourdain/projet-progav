name: C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install SDL2
        run: sudo apt-get install libsdl2-dev
      - name: Build
        run: sh build.sh
